<UserControl x:ConnectionId='1'
    x:Class="Kirana.App.Views.Controls.DeviceStatusWidget"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{ThemeResource SurfaceBrush}"
            BorderBrush="{ThemeResource SurfaceBorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="12,9">
        <Grid ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="32" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Width="32" Height="32" CornerRadius="16"
                    Background="{Binding Status, ElementName=Root, Converter={StaticResource HardwareStatusBackgroundConverter}}">
                <FontIcon Glyph="{Binding Glyph, ElementName=Root}" FontSize="13"
                          Foreground="{Binding Status, ElementName=Root, Converter={StaticResource HardwareStatusForegroundConverter}}" />
            </Border>
            <StackPanel Grid.Column="1" Spacing="1" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <TextBlock Text="{Binding Title, ElementName=Root}" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding Status, ElementName=Root}"
                               FontSize="11" FontWeight="SemiBold"
                               Foreground="{Binding Status, ElementName=Root, Converter={StaticResource HardwareStatusForegroundConverter}}" />
                </StackPanel>
                <TextBlock Text="{Binding Detail, ElementName=Root}" Style="{StaticResource CaptionTextStyle}"
                           TextTrimming="CharacterEllipsis" MaxWidth="260" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>

