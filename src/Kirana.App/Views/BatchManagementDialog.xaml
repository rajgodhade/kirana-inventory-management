<ContentDialog
    x:Class="Kirana.App.Views.BatchManagementDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:entities="using:Kirana.Domain.Entities"
    CloseButtonText="Close"
    DefaultButton="Close">

    <ContentDialog.Title>
        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="DialogTitleText" Grid.Column="0" Style="{ThemeResource TitleTextBlockStyle}" VerticalAlignment="Center" />
            <Button Grid.Column="1" Style="{StaticResource DialogCloseButtonStyle}"
                    Click="OnCloseIconClick" AutomationProperties.Name="Close dialog">
                <FontIcon Glyph="&#xE711;" FontSize="14" />
            </Button>
        </Grid>
    </ContentDialog.Title>

    <StackPanel Spacing="10" MinWidth="440" MaxHeight="580">
        <TextBlock Text="{x:Bind ViewModel.ProductName}" FontSize="18" FontWeight="SemiBold" />

        <TextBox Header="Batch number" Text="{x:Bind ViewModel.BatchNumber, Mode=TwoWay}" />

        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <CalendarDatePicker Grid.Column="0" Header="Manufacturing date" Date="{x:Bind ViewModel.ManufacturingDate, Mode=TwoWay}" />
            <CalendarDatePicker Grid.Column="1" Header="Expiry date" Date="{x:Bind ViewModel.ExpiryDate, Mode=TwoWay}" />
        </Grid>

        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Header="Quantity" Text="{x:Bind ViewModel.QuantityText, Mode=TwoWay}" />
            <TextBox Grid.Column="1" Header="Purchase price" Text="{x:Bind ViewModel.PurchasePriceText, Mode=TwoWay}" />
            <TextBox Grid.Column="2" Header="Selling price" Text="{x:Bind ViewModel.SellingPriceText, Mode=TwoWay}" />
        </Grid>

        <Button Content="Add Batch" Style="{StaticResource AccentButtonStyle}"
                Command="{x:Bind ViewModel.AddBatchCommand}" HorizontalAlignment="Left" />

        <InfoBar IsOpen="{x:Bind ViewModel.ErrorMessage, Mode=OneWay, Converter={StaticResource StringToBoolConverter}}"
                 Severity="Error" Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" />

        <TextBlock Text="Existing batches" FontWeight="SemiBold" Margin="0,8,0,0" />
        <ListView ItemsSource="{x:Bind ViewModel.Batches}" MaxHeight="200">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="entities:ProductBatch">
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind BatchNumber}" FontSize="12" />
                        <TextBlock Grid.Column="1" Text="{x:Bind ExpiryDate}" FontSize="12" />
                        <TextBlock Grid.Column="2" Text="{x:Bind Quantity}" FontSize="12" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</ContentDialog>
