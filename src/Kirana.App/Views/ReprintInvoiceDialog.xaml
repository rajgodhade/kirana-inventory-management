<ContentDialog
    x:Class="Kirana.App.Views.ReprintInvoiceDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Reprint Invoice"
    PrimaryButtonText="View &amp; Print"
    CloseButtonText="Cancel"
    DefaultButton="Primary">

    <StackPanel Spacing="10" MinWidth="380">

        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" PlaceholderText="Invoice number, e.g. INV-2026-000001"
                     Text="{x:Bind ViewModel.InvoiceNumberText, Mode=TwoWay}" KeyDown="OnInvoiceNumberKeyDown" />
            <Button Grid.Column="1" Content="Search" Click="OnSearchClick" />
        </Grid>

        <InfoBar IsOpen="{x:Bind ViewModel.ErrorMessage, Mode=OneWay, Converter={StaticResource StringToBoolConverter}}"
                 Severity="Error" Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" />

        <Border Visibility="{x:Bind ViewModel.HasResult, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="4" Padding="12">
            <TextBlock Text="{x:Bind ViewModel.ResultSummary, Mode=OneWay}" TextWrapping="Wrap" />
        </Border>

        <TextBlock Text="Reprinting requires manager/owner authorization." Opacity="0.7" FontSize="12" />
    </StackPanel>
</ContentDialog>
