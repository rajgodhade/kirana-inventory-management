<Page
    x:Class="Kirana.App.Views.SalesReturnDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:print="using:Kirana.Application.Printing">

    <Grid Background="{ThemeResource PageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Padding="{StaticResource PageHeaderPadding}" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Spacing="2">
                <TextBlock x:Name="TitleText" Style="{StaticResource PageTitleTextStyle}" />
                <TextBlock x:Name="SubtitleText" Style="{StaticResource PageSubtitleTextStyle}" />
            </StackPanel>
            <Button Grid.Column="1" Click="OnPrintClick" VerticalAlignment="Center" AutomationProperties.Name="Print Receipt">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE749;" FontSize="14" />
                    <TextBlock Text="Print Receipt" />
                </StackPanel>
            </Button>
        </Grid>

        <ScrollViewer Grid.Row="1">
            <StackPanel Padding="24,0,24,24" Spacing="16" MaxWidth="900" HorizontalAlignment="Left">

                <InfoBar x:Name="ErrorBar" IsOpen="False" Severity="Error" />

                <Border Style="{StaticResource CardBorderStyle}">
                    <Grid ColumnSpacing="32" RowSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0">
                            <TextBlock Text="Value of goods" Style="{StaticResource MetricLabelTextStyle}" />
                            <TextBlock x:Name="TotalText" Style="{StaticResource MetricTextStyle}" FontSize="22" />
                        </StackPanel>
                        <StackPanel Grid.Row="0" Grid.Column="1">
                            <TextBlock Text="Refunded" Style="{StaticResource MetricLabelTextStyle}" />
                            <TextBlock x:Name="RefundText" Style="{StaticResource MetricTextStyle}" FontSize="22" />
                        </StackPanel>
                        <StackPanel Grid.Row="0" Grid.Column="2">
                            <TextBlock Text="Method" Style="{StaticResource MetricLabelTextStyle}" />
                            <TextBlock x:Name="MethodText" Style="{StaticResource SectionTitleTextStyle}" />
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Spacing="4">
                            <TextBlock x:Name="CustomerText" Style="{StaticResource SecondaryTextStyle}" />
                            <TextBlock x:Name="ReasonText" Style="{StaticResource SecondaryTextStyle}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Grid>
                </Border>

                <StackPanel Spacing="10">
                    <TextBlock Text="Returned items" Style="{StaticResource SectionTitleTextStyle}" />
                    <Border Style="{StaticResource CardBorderStyle}" Padding="0">
                        <ItemsControl x:Name="LinesList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="print:ReturnReceiptLine">
                                    <Grid Padding="14,12" ColumnSpacing="16"
                                          BorderBrush="{ThemeResource SurfaceBorderBrush}" BorderThickness="0,0,0,1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" MinWidth="200" />
                                            <ColumnDefinition Width="120" />
                                            <ColumnDefinition Width="170" />
                                            <ColumnDefinition Width="120" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Spacing="2">
                                            <TextBlock Text="{x:Bind ProductName}" Style="{StaticResource CardTitleTextStyle}" TextWrapping="Wrap" />
                                            <TextBlock Text="{x:Bind ProductCode}" Style="{StaticResource CodeTextStyle}" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Style="{StaticResource BodyTextStyle}" VerticalAlignment="Center">
                                            <Run Text="{x:Bind Quantity, Converter={StaticResource QuantityConverter}}" /><Run Text=" " /><Run Text="{x:Bind Unit}" />
                                        </TextBlock>
                                        <TextBlock Grid.Column="2" Text="{x:Bind Disposition}" Style="{StaticResource SecondaryTextStyle}" VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="3" Text="{x:Bind LineRefundAmount, Converter={StaticResource CurrencyConverter}}"
                                                   Style="{StaticResource AmountTextStyle}" VerticalAlignment="Center" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
