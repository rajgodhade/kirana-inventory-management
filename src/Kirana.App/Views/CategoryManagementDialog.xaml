<ContentDialog
    x:Class="Kirana.App.Views.CategoryManagementDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:entities="using:Kirana.Domain.Entities"
    Title="Manage Categories"
    CloseButtonText="Done"
    DefaultButton="Close">

    <StackPanel Spacing="10" MinWidth="360" MaxHeight="480">
        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" PlaceholderText="New category name" Text="{x:Bind ViewModel.NewCategoryName, Mode=TwoWay}" />
            <Button Grid.Column="1" Content="Add" Command="{x:Bind ViewModel.AddCommand}" />
        </Grid>

        <InfoBar IsOpen="{x:Bind ViewModel.ErrorMessage, Mode=OneWay, Converter={StaticResource StringToBoolConverter}}"
                 Severity="Error" Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" />

        <ListView ItemsSource="{x:Bind ViewModel.Categories}" MaxHeight="360">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="entities:Category">
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind Name}" VerticalAlignment="Center" />
                        <Button Grid.Column="1" Content="Toggle Active" Click="OnToggleActiveClick" Tag="{x:Bind}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</ContentDialog>
